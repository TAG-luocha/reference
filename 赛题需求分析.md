# 嵌入式系统驱动程序开发竞赛 — 赛题需求分析

> 学号：250103243　　姓名：曹天赐
> 竞赛时长：180分钟　　总分：100分
> 硬件平台：keysking波特律动 STM32F103C8T6 学习板

---

## 一、竞赛总则

### 1.1 硬性约束

| 约束项 | 说明 |
|---|---|
| 烧录次数 | **只能烧录一次**，所有5个任务在一个综合程序中完成 |
| 任务切换 | 通过**按键**或**串口**切换演示任务 |
| 演示次数 | 每个任务演示次数 **≤2次** |
| 界面要求 | 清晰稳定，便于裁判评判 |
| 复位限制 | 最多复位 **2次**，每次复位 **扣3分** |
| 非阻塞运行 | 所有任务必须**非阻塞**，禁止在主循环中大量使用 `HAL_Delay()` |
| 硬件限制 | 禁止虚构引脚，禁止使用本板没有的 DAC、语音芯片、彩屏、SD 卡等 |

### 1.2 程序结构约束

| 约束项 | 说明 |
|---|---|
| 库 | **必须使用 STM32 HAL 库**和 CMSIS 标准接口 |
| 预处理宏 | 必须包含 `USE_HAL_DRIVER` 和 `STM32F103xB` |
| 编译器兼容 | 同时兼容 Keil MDK5 下 **ARM Compiler 5** 和 **ARM Compiler 6.7/ARMCLANG** |
| 语法限制 | 不允许使用只兼容 GCC 的语法 |
| 兼容宏 | `__attribute__((packed))`、`__weak`、裸内联汇编必须通过统一兼容宏封装 |
| C/C++ 混合编译 | 所有公共头文件必须使用 `extern "C"` 保护 |
| C99 标准 | 代码应尽量采用 C99 标准写法 |

### 1.3 工程目录结构规范（强制）

```
工程根目录/
└── user/
    ├── app/
    │   ├── inc/  # 【重要】此文件夹仅存放 .c 后缀的源文件
    │   └── src/  # 【重要】此文件夹仅存放 .h 后缀的头文件
    └── bsp/
        ├── inc/  # 【重要】此文件夹仅存放 .c 后缀的源文件
        └── src/  # 【重要】此文件夹仅存放 .h 后缀的头文件
```

> ⚠️ `inc/` 存 `.c` 源文件、`src/` 存 `.h` 头文件，与行业惯例相反！

### 1.4 分层调用规则

| 层 | 职责 | 允许 | 禁止 |
|---|---|---|---|
| **BSP 层** | 直接与硬件寄存器交互，封装所有硬件操作 | 被 APP 层调用 | 不能调用 APP 层任何函数/变量 |
| **APP 层** | 业务逻辑实现 | 调用 BSP 层接口 | 不能直接操作硬件寄存器 |

- BSP 层函数命名统一使用 `bsp_xxx()` 前缀
- APP 层函数命名统一使用 `app_xxx()` 前缀

---

## 二、硬件引脚映射总表

### 2.1 板载资源引脚

| 外设 | 引脚 | STM32 外设 | 备注 |
|---|---|---|---|
| OLED | PB6 (SCL) / PB7 (SDA) | I²C1 | 地址 **0x7A** |
| AHT20 温湿度 | PB6 (SCL) / PB7 (SDA) | I²C1 | 地址 **0x70**，与 OLED 共用 I²C 总线 |
| RGB LED 红色 | **PB0** | GPIO 输出 | **低电平亮** |
| RGB LED 绿色 | **PA7** | GPIO 输出 | **低电平亮** |
| RGB LED 蓝色 | **PA6** | GPIO 输出 | **低电平亮** |
| 无源蜂鸣器 | **PB9** | TIM4_CH4 PWM | **必须用 PWM 驱动**，直接给高电平不会响 |
| USART2 TX | PA2 | USART2 | 串口通信 |
| USART2 RX | PA3 | USART2 | 串口通信 |
| KEY1 | **PB12** | GPIO 输入 | 主键（任务切换、确认） |
| KEY2 | **PB13** | GPIO 输入 | 辅键（循环切换、功能控制） |
| WS2812 | PB4 | GPIO 输出 | 板载但不用于赛题 |
| 外部模拟输入 | PB1 | ADC1_IN9 | 示波器信号输入（0～3000mV） |

### 2.2 RGB LED 颜色对照表（低电平有效）

| 颜色 | PB0 (红) | PA7 (绿) | PA6 (蓝) | 视觉效果 |
|---|---|---|---|---|
| 灭 | H | H | H | 灯灭 |
| 🔴 红 | **L** | H | H | 仅红色亮 |
| 🟡 黄 | **L** | **L** | H | 红+绿 = 黄 |
| 🩵 青 | H | **L** | **L** | 绿+蓝 = 青 |
| 🟣 紫 | **L** | H | **L** | 红+蓝 = 紫 |

---

## 三、逐任务详细分析

---

### 任务1：基础显示与计时开发（全局核心）

**任务描述：**

1. OLED 屏幕顶部居中显示："学号+姓名"（250103243 曹天赐）
2. OLED 倒计时显示，格式：`XX:YY`，XX 为分钟，YY 为秒
3. 倒计时范围：`10:00` 至 `00:00`，可以按键控制启动/暂停与滚屏与正常显示
4. **所有功能后台持续运行**，不影响其他任务测评；执行其他任务时倒计时不能停，返回任务1时显示当前倒计时时间

**需求分析：**

| 子项 | 具体要求 | 关键点 |
|---|---|---|
| 标题显示 | 顶部居中 "250103243 曹天赐" | 需要中文字库支持 |
| 倒计时格式 | XX:YY（分:秒） | 大字号显示 |
| 范围 | 10:00 → 00:00 | 到 00:00 停止 |
| 控制按键 | 启动/暂停 | 短按切换 |
| 滚屏显示 | 倒计时数字闪烁提示效果 | 秒变化时反色闪烁 |
| 后台运行 | 切到其他任务时继续计时 | **全局 volatile 变量** |

**难度等级：** ⭐⭐ (中低)
**全局影响：** 倒计时是所有任务的时间基准，任务2直接依赖它

---

### 任务2：GPIO 外设控制开发（依赖任务1）

**任务描述：**

1. 基于任务1计时速度控制 RGB 彩灯：
   - (1) 每间隔 2 秒：红、黄快速交替
   - (2) 到达整数分钟：青、紫交替闪烁 2 次，后返回红、黄闪烁
2. 蜂鸣器联动控制：计时秒值为 30s 时，蜂鸣器鸣叫，鸣叫次数为分钟值
   - 其他任务不鸣叫，只有本任务鸣叫
3. 可通过按键关闭本功能

**需求分析：**

| 子项 | 触发条件 | 行为 | 实现方式 |
|---|---|---|---|
| 红黄交替 | 每 2 秒 | RGB 在红/黄之间切换 | 读 `g_countdown.total_elapsed_ms`，2s 周期切换 |
| 青紫闪烁 | 整数分钟（秒=0） | 青、紫交替闪 2 次，然后回红黄 | 状态机：`FLASH_SPECIAL` → 计数 2 次 → `FLASH_NORMAL` |
| 蜂鸣器鸣叫 | 秒=30 | 鸣叫 N 次（N=当前分钟值） | PWM 频率设置 + 间歇控制 |
| 关闭功能 | 按键 | 停止 RGB 和蜂鸣器 | 标志位 `app_rgb_enabled` |

**难度等级：** ⭐⭐ (中低)
**依赖：** 完全依赖任务1的 `g_countdown` 全局变量

---

### 任务3：音乐播放

**任务描述：**

1. 可通过按键播放指定音符：哆（do）、来（re）、咪（mi）、发（fa）
2. 通过串口启动指定音乐播放 "莫愁乡"
3. 通过串口启动播报："智能控制，创新未来"

**需求分析：**

| 子项 | 触发方式 | 实现 |
|---|---|---|
| do/re/mi/fa | KEY1 短按循环播放 | do→re→mi→fa→静音，每按一次切换 |
| "莫愁乡" | 串口命令 `CMD:MUSIC:MOCHOU` | 读取旋律数组，PWM 频率匹配播放 |
| "智能控制，创新未来" | 串口命令 `CMD:BROADCAST` | 蜂鸣器提示音 + OLED/串口显示文本 |

**关键约束：**
- 仅有无源蜂鸣器，**无法真实语音播报**，"播报"用蜂鸣器提示音代替
- **旋律大概率在比赛中被替换** → 采用旋律数据/播放引擎分离方案，比赛现场只需改数据文件

**难度等级：** ⭐⭐⭐ (中等 — 因为旋律可能被替换，需要灵活架构)

---

### 任务4：温度传感器

**任务描述：**

1. AHT20 温湿度传感器数据，OLED 显示：
   - "当前温度：XX.X℃"，"湿度：XX.X%"
2. OLED 实时绘制数据变化曲线

**需求分析：**

| 子项 | 要求 | 实现 |
|---|---|---|
| 温湿度读数 | AHT20 数据 | I²C 读取（地址0x70，与OLED共用总线） |
| 显示格式 | XX.X℃ / XX.X% | OLED 字符显示 |
| 实时曲线 | OLED 128×64 绘制 | 环形缓冲区存储最近 N 个点，画线连接 |

**关键约束：**
- AHT20 和 OLED 共用 I²C1 (PB6/PB7)，需互斥访问
- AHT20 单次测量需约 75ms 转换时间
- OLED 分辨率仅 128×64，温湿度各分约一半屏幕

**难度等级：** ⭐⭐ (中低 — 例程完整，主要是 I²C 总线管理和曲线绘制)

---

### 任务5：简易示波器采集显示（最难点）

**任务描述：**

1. 硬件采集外部模拟输入信号，输入量程：0～3000mV，500Hz~25kHz
   - 支持对接任务 6 输出的正弦波形作为被测信号
2. 档位可调：
   - 电压档位：100mV/div ~ 500mV/div
   - 时基档位：10μs/div ~ 5ms/div
3. 串口交互指令：可触发单次采集 / 连续采集
4. 指标要求：
   - 电压测量误差 ≤ ±50mV
   - 周期测量误差 ≤ ±100μs
5. OLED 界面要求：
   - 顶部状态栏：时基 XX μs/div 档位 XX mV/div
   - 屏幕绘图区实时绘制被测波形
   - 屏幕完整展示 ≥ 2 个被测信号周期
   - 横轴标注采样时间 (μs)、纵轴标注电压 (mV)
6. 附加功能：自动测算被测信号频率、峰峰值电压，刷新在 OLED 状态栏

**需求分析：**

| 子项 | 要求 | 关键技术 |
|---|---|---|
| 模拟输入 | 0～3000mV, 500Hz～25kHz | ADC1_IN9 (PB1)，可能需要分压保护 |
| 采样触发 | **必须 TIM 触发 ADC + DMA 搬运** | 不能用 `HAL_ADC_PollForConversion()` |
| 电压档位 | 100/200/500 mV/div | 串口命令设置，OLED 绘制时缩放 |
| 时基档位 | 10/50/100/500μs/div, 1/5ms/div | TIM ARR 动态调整采样率 |
| 单次/连续采集 | 串口命令切换 | `CMD:SCOPE:SINGLE` / `CMD:SCOPE:CONT` / `CMD:SCOPE:STOP` |
| 电压误差 | ≤±50mV | ADC 校准 + 参考电压补偿 |
| 周期误差 | ≤±100μs | 过零检测法或自相关法 |
| 显示要求 | ≥2 周期，128×64 OLED | 128 水平像素，需根据时基做抽取/插值 |
| 自动测量 | 频率 + 峰峰值 | 过零检测算周期→频率，max-min 算峰峰值 |

**采样率与时基对应关系：**

| 时基档位 | 屏幕跨度 (10格) | 目标采样率 (20点/格) | TIM3 ARR (72MHz) |
|---|---|---|---|
| 10 μs/div | 100 μs | 2 MS/s | 35 → ≈2 MS/s |
| 50 μs/div | 500 μs | 400 kS/s | 179 → ≈400 kS/s |
| 100 μs/div | 1 ms | 200 kS/s | 359 → ≈200 kS/s |
| 500 μs/div | 5 ms | 40 kS/s | 1799 |
| 1 ms/div | 10 ms | 20 kS/s | 3599 |
| 5 ms/div | 50 ms | 4 kS/s | 17999 |

> ⚠️ F103 ADC 最高约 1 MS/s（14MHz ADCCLK ÷ 14 cycles），10μs/div 时的 2MS/s 超限。
> 高频段（25kHz）只能做到**基本可辨**，测量精度优先保证中低频。

**数据流：**
```
TIM3 (TRGO) → 触发 ADC1 (IN9/PB1) → EOC 触发 DMA1_CH1 → 双缓冲 (buf0[512]/buf1[512])
                                                        ↓
                                                  Half/Full 中断 → 数据处理 → OLED 波形渲染
```

**难度等级：** ⭐⭐⭐⭐⭐ (最高 — 涉及 TIM+ADC+DMA 联动、波形渲染、自动测量)

---

## 四、任务依赖关系

```
任务1（倒计时）
  │
  ├──→ 任务2（RGB+蜂鸣器）── 读取 g_countdown 触发事件
  │
  ├──→ 任务3（音乐播放）  ── 独立运行，串口触发
  │
  ├──→ 任务4（AHT20温湿度）── 独立运行，共用 I²C
  │
  └──→ 任务5（示波器）    ── 独立运行，ADC 触发
```

核心结论：**任务1 是时间骨架**，其他任务并行挂在上面。

---

## 五、参考资料清单

| 优先级 | 资料 | 用途 |
|---|---|---|
| 必须 | 〖I²C 总线〗OLED 屏幕例程 | OLED 初始化、显示字符串、画点/线/曲线 |
| 必须 | 〖I²C 总线〗AHT20 温湿度传感器例程 | 任务4 温湿度读取 |
| 必须 | 〖I²C 总线〗软件 I²C 库例程 | 硬件 I²C 冲突时的兜底方案 |
| 必须 | 〖GPIO〗按键控制例程 | KEY1/KEY2 控制 |
| 必须 | 〖GPIO〗外部中断例程 | 按键中断写法 |
| 必须 | 〖PWM〗彩色呼吸灯例程 | RGB 三色 LED 驱动 |
| 必须 | 〖PWM〗无源蜂鸣器例程 | 任务2 蜂鸣器鸣叫 |
| 必须 | 〖PWM〗无源蜂鸣器播放音乐例程 | 任务3 音符和旋律 |
| 必须 | 〖UART 串口〗循环缓冲区+命令解析例程 | 串口命令框架 |
| 必须 | 〖ADC 转换〗读取电位器电压例程 | 任务5 ADC 采样基础 |
| 建议 | 〖UART 串口〗任意数据的收发例程 | 串口调试 |
| 建议 | 〖Timer〗旋转编码器例程 | 可选的旋钮调参 |
| 建议 | 〖Timer〗超声波测距/基本定时器例程 | 定时器学习 |
| 强烈建议 | 〖综合项目〗出厂演示程序 | 多外设共存、FreeRTOS 整合参考 |
