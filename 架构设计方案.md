# 竞赛程序 — 架构设计方案

> 本文档记录所有技术决策、架构设计、实现方案细节。
> 与 `赛题需求分析.md` 配套阅读。

---

## 一、总体架构

### 1.1 技术栈

| 项目 | 选型 | 原因 |
|---|---|---|
| MCU | STM32F103C8T6 | 竞赛指定 |
| 库 | **STM32 HAL 库** | 赛题要求 |
| RTOS | **FreeRTOS (CMSIS-RTOS v2)** | 出厂演示已验证，多任务天然并行 |
| 编译器 | Keil MDK5, ARMCC5 + ARMCC6 双兼容 | 赛题要求 |
| 时基 | TIM2 (1ms) 替代 SysTick | 出厂演示默认方案，FreeRTOS 兼容 |

### 1.2 架构总览

```
┌──────────────────────────────────────────────────────────┐
│                       main.c                              │
│  HAL_Init() → SystemClock_Config() → MX_xxx_Init()      │
│  → osKernelInitialize() → MX_FREERTOS_Init()             │
│  → osKernelStart()                                        │
└──────────────────────────────────────────────────────────┘
                              │
         ┌────────────────────┼────────────────────┐
         ▼                    ▼                    ▼
┌─────────────┐    ┌─────────────┐    ┌─────────────────┐
│ i2cService  │    │  uiTask     │    │  counterTask    │
│ (I²C+OLED   │    │ (按键+状态机│    │  (倒计时逻辑)    │
│  渲染)      │    │  任务切换)  │    │                  │
└─────────────┘    └─────────────┘    └─────────────────┘
         ▲                    │                    │
         │ PostDisplayRequest │                    │ g_countdown
         │                    │                    ▼
┌────────┴──────┐    ┌───────┴──────┐    ┌─────────────────┐
│ app_countdown │    │ app_rgb_buzzer│    │  其他任务线程    │
│ app_aht20     │    │ app_music     │    │  (独立 osDelay)  │
│ app_scope     │    │               │    │                  │
└───────────────┘    └───────────────┘    └─────────────────┘
```

### 1.3 FreeRTOS 任务规划

| 任务名 | 优先级 | 周期 | 栈 (words) | 职责 |
|---|---|---|---|---|
| `i2cServiceTask` | 高 | 30ms | 1536 | I²C 总线操作、OLED 刷新队列处理 |
| `scopeTask` | 高 | ADC 中断驱动 | 2048 | ADC 数据处理 + 波形渲染 |
| `counterTask` | 中 | 100ms | 512 | 倒计时逻辑 + 任务1 显示 |
| `rgbBuzzerTask` | 中 | 50ms | 512 | 任务2 RGB 事件 + 蜂鸣器控制 |
| `musicTask` | 中 | 50ms | 1024 | 任务3 音符/旋律播放引擎 |
| `uartTask` | 中 | 串口中断驱动 | 1024 | 串口命令接收 + 解析 |
| `uiTask` | 中 | 50ms | 768 | 按键扫描 + UI 状态机 + 任务切换 |
| `aht20Task` | 中低 | 500ms | 512 | 任务4 AHT20 采样 + 曲线数据更新 |

> 总计约 8 个任务，F103C8T6 有 20KB SRAM，需合理分配堆栈。

---

## 二、工程目录结构

```
工程根目录/
├── Core/                          # CubeMX/CubeIDE 生成（HAL 配置）
│   ├── Inc/
│   │   ├── main.h
│   │   ├── stm32f1xx_hal_conf.h
│   │   ├── stm32f1xx_it.h
│   │   ├── FreeRTOSConfig.h
│   │   ├── gpio.h / i2c.h / tim.h / usart.h / dma.h
│   │   └── compiler_compat.h     # ARMCC5/6 兼容宏
│   ├── Src/
│   │   ├── main.c
│   │   ├── stm32f1xx_it.c
│   │   ├── system_stm32f1xx.c
│   │   ├── freertos.c
│   │   └── gpio.c / i2c.c / tim.c / usart.c / dma.c
│   └── Startup/
│       └── startup_stm32f103c8tx.s
├── Drivers/                       # HAL 库（从出厂演示复制）
│   ├── CMSIS/
│   └── STM32F1xx_HAL_Driver/
├── Middlewares/                   # FreeRTOS（从出厂演示复制）
│   └── Third_Party/FreeRTOS/
└── user/                          # 【业务代码 — 符合赛题目录规范】
    ├── app/
    │   ├── inc/                   # 【⚠️ 存 .c 源文件】
    │   │   ├── app.c              # 应用层主逻辑（状态机、任务分发）
    │   │   ├── app_countdown.c    # 任务1：倒计时
    │   │   ├── app_rgb_buzzer.c   # 任务2：RGB 彩灯 + 蜂鸣器
    │   │   ├── app_music.c        # 任务3：音乐播放
    │   │   ├── app_aht20.c        # 任务4：温湿度 + 曲线
    │   │   └── app_scope.c        # 任务5：示波器
    │   └── src/                   # 【⚠️ 存 .h 头文件】
    │       ├── app.h
    │       ├── app_countdown.h
    │       ├── app_rgb_buzzer.h
    │       ├── app_music.h
    │       ├── app_aht20.h
    │       ├── app_scope.h
    │       ├── task_registry.h    # 任务注册表
    │       └── melody_data.h      # 旋律数据定义
    └── bsp/
        ├── inc/                   # 【⚠️ 存 .c 源文件】
        │   ├── bsp.c              # BSP 初始化总入口
        │   ├── bsp_key.c          # 按键驱动（状态机消抖）
        │   ├── bsp_oled.c         # OLED 驱动（从 keysking 移植）
        │   ├── bsp_oled_font.c    # OLED 字库（含中文字模）
        │   ├── bsp_aht20.c        # AHT20 温湿度驱动（从 keysking 移植）
        │   ├── bsp_rgb.c          # RGB LED 驱动（PA6/PA7/PB0）
        │   ├── bsp_buzzer.c       # 蜂鸣器 PWM 旋律播放引擎
        │   ├── bsp_uart_cmd.c     # 串口 DMA+循环缓冲区+命令解析
        │   ├── bsp_adc_scope.c    # ADC+DMA 示波器采集
        │   └── bsp_i2c_service.c  # I²C 总线管理服务
        └── src/                   # 【⚠️ 存 .h 头文件】
            ├── bsp.h
            ├── bsp_key.h
            ├── bsp_oled.h
            ├── bsp_oled_font.h
            ├── bsp_aht20.h
            ├── bsp_rgb.h
            ├── bsp_buzzer.h
            ├── bsp_uart_cmd.h
            ├── bsp_adc_scope.h
            ├── bsp_i2c_service.h
            ├── pin_config.h       # 【核心】引脚集中配置 — 改动入口
            └── compiler_compat.h  # ARMCC5/6 兼容宏
```

---

## 三、引脚配置集中化 (`pin_config.h`)

**设计原则：** 所有硬件引脚宏定义集中在一个文件，换板子/改引脚只改这里。

```c
/* ========== I²C1 总线（OLED + AHT20 共用）========== */
#define I2C_BUS                 I2C1
#define I2C_SCL_PORT            GPIOB
#define I2C_SCL_PIN             GPIO_PIN_6
#define I2C_SDA_PORT            GPIOB
#define I2C_SDA_PIN             GPIO_PIN_7

/* ========== OLED ========== */
#define OLED_I2C_ADDR           0x7A
#define OLED_WIDTH              128
#define OLED_HEIGHT             64

/* ========== AHT20 ========== */
#define AHT20_I2C_ADDR          0x70

/* ========== RGB LED（三路独立，低电平有效）========== */
#define RGB_RED_PORT            GPIOB
#define RGB_RED_PIN             GPIO_PIN_0
#define RGB_GREEN_PORT          GPIOA
#define RGB_GREEN_PIN           GPIO_PIN_7
#define RGB_BLUE_PORT           GPIOA
#define RGB_BLUE_PIN            GPIO_PIN_6
#define RGB_ACTIVE_LEVEL        0   // 0=低电平亮, 1=高电平亮

/* ========== 按键 ========== */
#define KEY1_PORT               GPIOB
#define KEY1_PIN                GPIO_PIN_12
#define KEY2_PORT               GPIOB
#define KEY2_PIN                GPIO_PIN_13
#define KEY_PRESSED_LEVEL       GPIO_PIN_RESET  // 按下为低电平

/* ========== 蜂鸣器（无源，PWM 驱动）========== */
#define BUZZER_TIM              TIM4
#define BUZZER_TIM_CHANNEL      TIM_CHANNEL_4
#define BUZZER_PORT             GPIOB
#define BUZZER_PIN              GPIO_PIN_9
#define BUZZER_TIM_CLK_HZ       72000000  // TIM4 挂 APB1, 72MHz

/* ========== USART2 串口命令 ========== */
#define CMD_UART                USART2
#define CMD_UART_BAUDRATE       115200
#define CMD_UART_TX_PORT        GPIOA
#define CMD_UART_TX_PIN         GPIO_PIN_2
#define CMD_UART_RX_PORT        GPIOA
#define CMD_UART_RX_PIN         GPIO_PIN_3

/* ========== ADC 示波器 ========== */
#define SCOPE_ADC               ADC1
#define SCOPE_ADC_CHANNEL       ADC_CHANNEL_9  // PB1 → ADC1_IN9
#define SCOPE_ADC_PORT          GPIOB
#define SCOPE_ADC_PIN           GPIO_PIN_1
#define SCOPE_TRIG_TIM          TIM3           // TRGO 触发 ADC
#define SCOPE_VREF_MV           3300           // 参考电压 mV
#define SCOPE_ADC_RES           4096           // 12 位分辨率
#define SCOPE_INPUT_MAX_MV      3000           // 输入量程上限
#define SCOPE_VOLTAGE_DIVIDER   1.0f           // 分压系数（无分压=1.0）
```

---

## 四、核心模块设计方案

---

### 4.1 全局倒计时系统 (`app_countdown`)

**设计：** 全局 `volatile` 变量，一处更新，多处只读。

```c
/* 倒计时起始值 — 赛题改动入口 */
#define COUNTDOWN_START_MIN    10
#define COUNTDOWN_START_SEC    0

/* 全局倒计时状态 */
typedef struct {
    uint8_t  minute;               // 当前分钟 (0-10)
    uint8_t  second;               // 当前秒   (0-59)
    uint8_t  running;              // 1=运行中, 0=暂停
    uint8_t  scroll_mode;          // 1=闪烁显示, 0=正常显示
    uint32_t total_elapsed_ms;     // 总流逝毫秒数（任务2事件判断用）
} countdown_t;

extern volatile countdown_t g_countdown;
extern volatile uint32_t    g_sys_tick_ms;  // 系统毫秒计数器
```

**数据流：**
```
SysTick/TIM2 ISR (1ms)
  → g_sys_tick_ms++
  → counterTask (每100ms)
      → g_countdown.second--  (仅此一处写)
      → 各任务线程 (只读)
```

**闪烁效果实现：**
- 秒变化后的前 200ms 内，倒计时数字反色显示
- `g_countdown.scroll_mode` 控制是否启用闪烁
- 通过 KEY2 短按切换 ON/OFF

---

### 4.2 I²C 总线管理 (`bsp_i2c_service`)

**方案：单任务代理 + 消息队列**

```
┌──────────┐      ┌──────────┐      ┌──────────┐
│ task4    │      │ task1    │      │ others   │
│ AHT20    │      │ OLED     │      │ OLED     │
└────┬─────┘      └────┬─────┘      └────┬─────┘
     │ PostDisplay    │ PostDisplay     │ PostDisplay
     ▼                 ▼                 ▼
  ┌────────────────────────────────────────────┐
  │           OLED_Disp_Queue                   │
  │      (FreeRTOS Queue, 深度=4)               │
  └──────────────────┬─────────────────────────┘
                     ▼
  ┌────────────────────────────────────────────┐
  │          i2cServiceTask                     │
  │  · 处理队列中的 OLED 渲染请求               │
  │  · 每 500ms 读取一次 AHT20                  │
  │  · 唯一操作 I²C 总线的任务                  │
  └────────────────────────────────────────────┘
```

```c
/* 显示请求回调 */
typedef void (*draw_func_t)(void);

/**
 * 提交显示请求
 * @param draw_fn   绘制回调（在 i2cServiceTask 上下文中执行）
 * @param priority  1=高优先, 0=普通
 * 
 * 限制: draw_fn 内禁止阻塞 >5ms，禁止递归调用 PostDisplayRequest()
 */
void PostDisplayRequest(draw_func_t draw_fn, uint8_t priority);
```

---

### 4.3 按键驱动 (`bsp_key`)

**方案：定时扫描 10ms + 独立状态机**

```
状态机:
IDLE → 消抖(30ms) → 按下计时 → 短按/长按判定 → 等待释放 → IDLE
```

**阈值：**

| 参数 | 值 | 说明 |
|---|---|---|
| 扫描周期 | 10ms | 在 uiTask 中调用 |
| 消抖时间 | 30ms (3 ticks) | |
| 长按阈值 | **800ms (80 ticks)** | |
| 选择模式超时 | **5s** | 无操作自动退出 |

```c
typedef struct {
    uint8_t  state;               // 状态机当前状态
    uint16_t press_ticks;         // 按下计时 (单位:10ms)
    uint8_t  short_flag;          // 短按事件标志 (上层消费后清零)
    uint8_t  long_flag;           // 长按事件标志 (上层消费后清零)
    uint8_t  is_long_triggered;   // 防止长按触发了又触发短按
} key_state_t;

void BSP_Key_Scan(key_state_t *key, uint8_t current_level);
```

---

### 4.4 UI 状态机 — 任务切换 (`uiTask`)

```
┌────────────────┐    KEY1长按 (>800ms)   ┌──────────────────┐
│  MODE_NORMAL   │ ────────────────────→  │ MODE_SELECTING   │
│                │                        │                  │
│  task_id = N   │ ←─── KEY1短按(确认) ── │ 显示任务菜单      │
│  · KEY1短按→任  │     / 5s超时退出       │ · KEY2短按→循环   │
│    务内动作    │                        │ · KEY1短按→确认   │
│  · KEY2短按→任  │                        │ · KEY1长按→退出   │
│    务内动作2   │                        │    不切换任务     │
└────────────────┘                        └──────────────────┘
```

**各任务在 NORMAL_MODE 下的按键行为：**

| 任务 | KEY1 短按 | KEY2 短按 |
|---|---|---|
| 任务1 倒计时 | 启动/暂停倒计时 | 滚屏（闪烁）开/关 |
| 任务2 RGB+蜂鸣器 | 关闭/开启 RGB+蜂鸣器 | 无 |
| 任务3 音乐 | 循环播放 do→re→mi→fa→停 | 无 |
| 任务4 温湿度 | 无（自动显示） | 无 |
| 任务5 示波器 | 单次采集触发 | 暂停/恢复连续采集 |

**选择模式 OLED 菜单界面：**
```
  ┌──────────────────────┐
  │  选择任务 (KEY2 切换) │
  │                      │
  │  > 任务1: 倒计时      │
  │    任务2: RGB 彩灯    │
  │    任务3: 音乐播放    │
  │    任务4: 温湿度      │
  │    任务5: 示波器      │
  │                      │
  │  KEY1 确认  KEY2 切换  │
  └──────────────────────┘
```

---

### 4.5 任务注册表 (`task_registry`)

**设计：** 任务增删只需改一个结构体数组。

```c
typedef enum {
    TASK_1_COUNTDOWN = 0,
    TASK_2_RGB_BUZZER,
    TASK_3_MUSIC,
    TASK_4_AHT20,
    TASK_5_SCOPE,
    TASK_COUNT              // 自动计数，不要手动修改
} task_id_t;

typedef struct {
    task_id_t   id;
    const char *name;            // OLED 菜单显示名
    void      (*init)(void);     // 进入任务时初始化
    void      (*show)(void);     // OLED 显示回调
    void      (*update)(void);   // 逻辑更新回调
    void      (*exit)(void);     // 离开任务时清理
    void      (*key1_short)(void);
    void      (*key2_short)(void);
} task_entry_t;

/* 任务注册表 — 【比赛增删任务只改这里】 */
static const task_entry_t task_registry[TASK_COUNT] = {
    [TASK_1_COUNTDOWN] = {
        .id = TASK_1_COUNTDOWN,
        .name = "任务1: 倒计时",
        .init = app_countdown_init,
        .show = app_countdown_draw,
        .update = app_countdown_update,
        .key1_short = app_countdown_key1,
        .key2_short = app_countdown_key2,
    },
    // ... 其他任务同理
};
```

---

### 4.6 任务2：RGB + 蜂鸣器事件驱动

**设计：** 基于 `g_countdown` 的秒变化事件触发。

```c
typedef enum {
    RGB_NORMAL,          // 正常红黄交替
    RGB_SPECIAL_FLASH,   // 整数分钟青紫闪烁
} rgb_state_t;

/* 每 50ms 调用的更新逻辑 */
void app_rgb_update(void)
{
    static uint8_t last_second = 0xFF;
    uint8_t sec = g_countdown.second;
    uint8_t min = g_countdown.minute;

    if (sec == last_second) return;  // 秒未变化，跳过
    last_second = sec;

    // 1. RGB: 每 2 秒红黄交替
    if (sec % 2 == 0) {
        BSP_RGB_SetColor(RGB_RED);   // 偶数秒 → 红
    } else {
        BSP_RGB_SetColor(RGB_YELLOW); // 奇数秒 → 黄
    }

    // 2. RGB: 整数分钟 — 青紫闪烁 2 次（状态机处理）
    if (sec == 0) {
        g_rgb_state = RGB_SPECIAL_FLASH;
        g_rgb_flash_count = 0;
    }

    // 3. 蜂鸣器: 秒=30 鸣叫 N=min 次
    if (sec == 30) {
        BSP_Buzzer_BeepTimes(min);  // 非阻塞，用状态机分次鸣叫
    }
}
```

---

### 4.7 任务3：旋律播放 — 数据/引擎分离

**核心思想：** 旋律数据（`melody_data.h/.c`）和播放引擎（`bsp_buzzer.c`）完全分离。
比赛时换旋律只需改数据文件，不需要理解播放逻辑。

**音符频率表（A4=440Hz 十二平均律）：**

| 简谱 | 低音 (Hz) | 中音 (Hz) | 高音 (Hz) |
|---|---|---|---|
| 1 (do) | 262 | 523 | 1047 |
| 2 (re) | 294 | 587 | 1175 |
| 3 (mi) | 330 | 659 | 1319 |
| 4 (fa) | 349 | 698 | 1397 |
| 5 (sol) | 392 | 784 | 1568 |
| 6 (la) | 440 | 880 | 1760 |
| 7 (si) | 494 | 988 | 1976 |

**时值定义 (BPM=120 基准)：**

| 时值 | 宏 | 毫秒 |
|---|---|---|
| 四分音符（一拍） | `DUR_1` | 500 |
| 八分音符（半拍） | `DUR_1_2` | 250 |
| 十六分音符 | `DUR_1_4` | 125 |
| 二分音符（两拍） | `DUR_2` | 1000 |
| 附点四分音符 | `DUR_1_DOT` | 750 |
| 全音符（四拍） | `DUR_4` | 2000 |

**旋律数据结构：**
```c
typedef struct {
    uint16_t freq;       // 频率 Hz, NOTE_REST=休止, NOTE_END=结束
    uint16_t duration;   // 持续时间 ms
} melody_note_t;

/* 示例：do/re/mi/fa 的单音旋律 */
const melody_note_t MELODY_NOTE_DO[] = { { NOTE_DO, 300 }, { NOTE_END, 0 } };
```

**串口命令触发注册表：**
```c
static const melody_cmd_entry_t g_melody_table[] = {
    { "MOCHOU",    MELODY_MOCHOUXIANG    },
    { "BROADCAST", MELODY_BROADCAST_BEEP },
    // 【比赛加新曲】在这里加一行即可
    // { "CUSTOM1",  MELODY_CUSTOM_1       },
};
```

**比赛换旋律操作步骤：**
1. 在 `melody_data.h` 末尾添加新旋律数组声明
2. 在 `melody_data.c` 中用简谱填入 `{音符, 时值}` 对
3. 在 `g_melody_table[]` 加一行注册
4. 编译，串口发 `CMD:MUSIC:xxx` 即可播放

---

### 4.8 任务5：示波器 — ADC+DMA 采集方案

**数据流：**
```
TIM3 (PWM模式, TRGO=Update)
    │ TRGO 触发
    ▼
ADC1 (CH9, PB1, External Trigger TIM3_TRGO)
    │ EOC (转换完成)
    ▼
DMA1_CH1 (Circular 模式, 半传输/全传输中断)
    │
    ▼
双缓冲: buf0[512] / buf1[512]
    │ HalfCplt / Cplt 中断
    ▼
scopeTask: 数据处理 → 峰值检测 → 自动测量 → OLED 波形渲染
```

**时基档位与 TIM3 配置对应表：**

| 时基档位 | 采样率 | TIM3 ARR (72MHz) | TIM3 PSC | 备注 |
|---|---|---|---|---|
| 10 μs/div | 500 kS/s | 143 | 0 | F103 ADC 极限附近 |
| 50 μs/div | 400 kS/s | 179 | 0 | |
| 100 μs/div | 200 kS/s | 359 | 0 | |
| 500 μs/div | 40 kS/s | 1799 | 0 | |
| 1 ms/div | 20 kS/s | 3599 | 0 | |
| 5 ms/div | 4 kS/s | 17999 | 0 | |

**电压档位（OLED Y 轴缩放）：**

| 电压档位 | 屏幕对应 | Y 轴范围 |
|---|---|---|
| 100 mV/div | 50 像素/div (8格) | 0～800 mV |
| 200 mV/div | 25 像素/div | 0～1600 mV |
| 500 mV/div | 10 像素/div | 0～4000 mV |

**自动测量算法：**
- **频率**：过零检测法，找到两个连续上升过零点，时间差 = 周期 → 频率 = 1/周期
- **峰峰值**：缓冲区 max - min，换算为电压

---

### 4.9 串口命令协议 (`bsp_uart_cmd`)

**统一格式：** `CMD:<action>[:param]\r\n`

**命令表：**

| 命令 | 功能 | 回复 |
|---|---|---|
| `CMD:TASK:1` ~ `CMD:TASK:5` | 切换任务 | `OK\r\n` |
| `CMD:COUNTDOWN:START` | 启动倒计时 | `OK\r\n` |
| `CMD:COUNTDOWN:PAUSE` | 暂停倒计时 | `OK\r\n` |
| `CMD:MUSIC:MOCHOU` | 播放"莫愁乡" | `OK\r\n` |
| `CMD:MUSIC:CUSTOM1` | 播放备用旋律1 | `OK\r\n` |
| `CMD:BROADCAST` | 播报提示音 | `OK\r\n` |
| `CMD:SCOPE:SINGLE` | 示波器单次采集 | `OK\r\n` |
| `CMD:SCOPE:CONT` | 示波器连续采集 | `OK\r\n` |
| `CMD:SCOPE:STOP` | 示波器停止采集 | `OK\r\n` |
| `CMD:SCOPE:TBASE:100` | 时基设为 100μs/div | `OK\r\n` |
| `CMD:SCOPE:VDIV:200` | 电压设为 200mV/div | `OK\r\n` |
| 未知/错误命令 | | `ERR:UNKNOWN\r\n` |

**实现：DMA + 循环缓冲区**
- USART2 RX 使用 DMA 循环模式接收
- 环形缓冲区存储原始字节
- `uartTask` 定期扫描缓冲区，检测 `\r\n` 分隔符
- 提取完整命令行后解析执行

---

### 4.10 OLED 中文字库 (`bsp_oled_font`)

**方案：提取式字库，约 150-200 个常用汉字**

| 参数 | 值 |
|---|---|
| 字体大小 | 16×16 像素 |
| 每字数据量 | 32 字节 |
| 目标字数 | 150～200 字 |
| 总存储 | 约 5～6 KB |
| Flash 总容量 | 64 KB |
| Flash 可用 | HAL + FreeRTOS 约占 30-40KB，字库放得下 |

**取模工具：** PCtoLCD2002（逐行式，高位在前）

**需包含的汉字：** 任务、选、择、时、倒、计、彩、灯、蜂、鸣、器、音、乐、播、放、温、湿、度、曲、线、简、示、波、确、认、切、换、开、始、暂、停、关、闭、启、正、常、滚、动、显、示、返、回、退、出、当、前、学、号、姓、名、莫、愁、乡、智、能、控、制、创、新、未、来、毫、伏、微、秒、摄、氏、频、率、峰、值、电、压、档、位、基、单、次、连、续、停、止、采、集、曹、天、赐等

**找不到的字处理：** 显示 `□` (全黑方块) 替代

---

## 五、编译器兼容 (`compiler_compat.h`)

**设计原则：** 优先使用 CMSIS 标准函数规避差异，只在绕不开的地方用兼容宏。

**提供的 8 个核心宏：**

| 宏 | 用途 | 使用位置 |
|---|---|---|
| `PACKED_STRUCT_BEGIN/END` | 紧凑结构体 | 通信协议帧定义 |
| `WEAK` | 弱符号函数 | 中断回调覆盖 |
| `STATIC_INLINE` | 内联函数 | 头文件工具函数 |
| `ALIGNED(n)` | 内存对齐 | DMA 缓冲区 |
| `UNUSED(x)` | 抑制未使用警告 | 回调参数 |
| `ASM_CODE/ASM_SINGLE` | 内联汇编 | 极少使用,优先用 CMSIS |
| `extern "C"` 保护 | C/C++ 混合编译 | 所有公共头文件 |
| 位带操作宏 | 原子性位操作 | GPIO 高速翻转 |

**使用原则：**
1. 所有 `.h` 文件必须 `#include "compiler_compat.h"`
2. 业务代码不直接写 `__attribute__`、`__packed`、内联汇编
3. 中断管理优先用 `__disable_irq()`/`__enable_irq()`，不写内联汇编

---

## 六、BSP 层模块接口规范

| 模块 | 主要接口 | 说明 |
|---|---|---|
| `bsp_key` | `BSP_Key_Scan()` | 10ms 周期调用，填充事件标志 |
| `bsp_oled` | `OLED_Init()`, `OLED_NewFrame()`, `OLED_ShowFrame()`, `OLED_PrintString()`, `OLED_DrawLine()`, `OLED_DrawCircle()`, `OLED_DrawRectangle()` | 从 keysking OLED 例程移植 |
| `bsp_aht20` | `AHT20_Init()`, `AHT20_Read(&T, &H)` | 从 keysking AHT20 例程移植 |
| `bsp_rgb` | `BSP_RGB_SetColor(color)`, `BSP_RGB_Off()` | 智能映射 PA6/PA7/PB0 电平 |
| `bsp_buzzer` | `BSP_Buzzer_PlayMelody(melody)`, `BSP_Buzzer_PlayNote(freq, dur)`, `BSP_Buzzer_BeepTimes(n)`, `BSP_Buzzer_Stop()` | 非阻塞播放引擎 |
| `bsp_uart_cmd` | `BSP_UART_Cmd_Init()`, `BSP_UART_Cmd_Process()` | DMA+环形缓冲区+命令解析 |
| `bsp_adc_scope` | `BSP_Scope_Init()`, `BSP_Scope_Start()`, `BSP_Scope_Stop()`, `BSP_Scope_SetTimebase()`, `BSP_Scope_SetVoltageDiv()` | TIM+ADC+DMA 三联动 |
| `bsp_i2c_service` | `PostDisplayRequest(draw_fn, prio)` | 唯一的 I²C 入口 |

---

## 七、APP 层模块接口规范

| 模块 | 主要接口 | 说明 |
|---|---|---|
| `app_countdown` | `app_countdown_init()`, `app_countdown_draw()`, `app_countdown_update()`, `app_countdown_key1()`, `app_countdown_key2()` | 倒计时业务逻辑 |
| `app_rgb_buzzer` | `app_rgb_init()`, `app_rgb_draw()`, `app_rgb_update()`, `app_rgb_key1()`, `app_rgb_exit()` | RGB+蜂鸣器事件 |
| `app_music` | `app_music_init()`, `app_music_draw()`, `app_music_update()`, `app_music_key1()` | 旋律播放 |
| `app_aht20` | `app_aht20_init()`, `app_aht20_draw()`, `app_aht20_update()` | 温湿度+曲线 |
| `app_scope` | `app_scope_init()`, `app_scope_draw()`, `app_scope_update()`, `app_scope_key1()`, `app_scope_key2()`, `app_scope_exit()` | 示波器界面 |
| `app` | `app_init()`, `app_process()` | 全局状态机，在 uiTask 中运行 |

---

## 八、代码注释规范

每个文件头部格式：
```c
/**
 * ============================================================
 * @file        xxx.c
 * @brief       功能简述
 * @author      曹天赐 (250103243)
 * @date        2026-06-06
 *
 * 【文件职责】这个文件负责什么
 * 【依赖关系】调用了哪些模块，被哪些模块调用
 * 【赛题变化时的修改指南】如果某某需求变了，改哪里
 * 【已知限制】当前版本的已知不足
 * ============================================================
 */
```

函数注释格式：
```c
/**
 * @brief  函数功能简述
 * @param  param1 参数1说明（取值范围、单位）
 * @param  param2 参数2说明
 * @retval 返回值说明
 * @note   调用时机、注意事项
 *         【修改指南】如需修改行为，改这里
 */
```

---

## 九、已知待定项

| 项目 | 状态 | 说明 |
|---|---|---|
| "莫愁乡"具体简谱 | ⚠️ 待填入 | 旋律数据结构已就位，填入音符数组即可 |
| 示波器高频精度 | ✅ 已接受 | 25kHz 基本可辨，中低频保证精度 |
| WS2812 (PB4) | 🔵 保留 | 驱动可复用出厂演示代码，但不用于赛题 |
| 备用旋律槽位 | 🔵 就绪 | `MELODY_CUSTOM_1/2` 已声明，比赛填数据即可 |
| 裁判可能的新需求 | 🔵 架构已预留 | pin_config.h + task_registry + melody_data，三处入口应对变化 |
